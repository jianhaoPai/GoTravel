module.exports = {
  // Fill this after enabling CloudBase in WeChat DevTools.
  cloudEnvId: '',

  // Keep mock data available before CloudBase is configured.
  useMockDataWhenNoCloud: true,

  // Keep false after cloudEnvId is set, so cloud permission/function errors are visible.
  fallbackToMockOnCloudError: false
};
